# Grafana сам підхоплює це джерело даних при старті (стадія 7) - клікати нічого не треба.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
